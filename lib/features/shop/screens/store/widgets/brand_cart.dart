import 'package:flutter/material.dart';

class BrandCart extends StatelessWidget {
  const BrandCart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
